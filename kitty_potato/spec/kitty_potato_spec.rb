require 'spec_helper'

describe KittyPotato do
  it 'has a version number' do
    expect(KittyPotato::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
